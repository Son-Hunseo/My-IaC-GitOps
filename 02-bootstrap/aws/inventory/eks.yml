---
# 기존 EKS 클러스터에 접속할 로컬 실행 호스트 인벤토리입니다.
# AWS 인프라 생성은 01-infra 단계에서 끝났다고 가정하고, 이 인벤토리는
# Argo CD add-on 설치에만 사용합니다.
#
# 변수는 group_vars/all/aws.yml 한 파일에서 관리합니다.
all:
  children:
    eks_admin:
      hosts:
        localhost:
          ansible_connection: local
