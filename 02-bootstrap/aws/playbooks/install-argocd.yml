---
# 이미 생성되어 접근 가능한 EKS 클러스터에 Argo CD를 설치합니다.
- name: Install Argo CD on existing AWS EKS cluster
  hosts: eks_admin
  become: false
  gather_facts: false
  roles:
    - role: argocd
      tags:
        - argocd
