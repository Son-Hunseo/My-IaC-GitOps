---
# kubespray로 만든 온프렘 클러스터에 Argo CD add-on을 설치합니다.
# control plane 노드에서 kubespray가 만든 admin.conf로 kubectl을 실행합니다.
- name: Install Argo CD add-on on on-premise cluster
  hosts: kube_control_plane[0]
  become: true
  gather_facts: false
  roles:
    - role: argocd
      tags:
        - argocd
