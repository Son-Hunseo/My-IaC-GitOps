---
# 모든 Kubernetes 노드에서 OS 디스크 확장과 Kubernetes 사전 조건을 구성합니다.
- name: Prepare Kubernetes nodes
  hosts: kube_control_plane:kube_workers
  become: true
  gather_facts: true
  roles:
    - role: disk-extend
      when: disk_extend_enabled | bool
    - role: k8s-prereq

