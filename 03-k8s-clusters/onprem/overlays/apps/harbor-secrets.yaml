apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: onprem-harbor-secrets
  namespace: argocd
  annotations:
    # Vault ClusterSecretStore가 준비된 뒤 Harbor 고정 Secret을 생성합니다.
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: platform
  source:
    repoURL: https://github.com/Son-Hunseo/My-GitAIOps
    targetRevision: main
    path: 03-k8s-clusters/onprem/overlays/harbor-secrets
  destination:
    server: https://kubernetes.default.svc
    namespace: harbor
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 5m
