# Gateway API CRD는 02-bootstrap의 kubespray add-on(`gateway_api_enabled: true`)이
# 클러스터 설치 시점에 이미 넣어 둡니다. chart는 CRD를 만들지 않습니다.
nginxGateway:
  name: nginx-gateway

# Gateway가 생성하는 NGINX data plane Service는 On-Premise MetalLB IP를 받을 수 있도록 LoadBalancer로 둡니다.
nginx:
  service:
    type: LoadBalancer
